# 훅 성능 기준선

훅은 모든 `Write`·`Edit`마다 실행된다. 규칙을 늘리기 전에 현재 비용을 측정하고, 이후 같은
조건으로 다시 재어 회귀 여부를 비교한다.

이 문서의 수치는 **합성 fixture와 메모리에서 생성한 합성 소스 기준**이다. 실제 코드베이스의
성능을 나타내지 않으며 실제 코드나 개인정보를 사용하지 않았다. (D-07, D-08)

## 재현 방법

```bash
python3 tests/benchmark_hooks.py
python3 tests/benchmark_hooks.py --json
```

`tests/benchmark_hooks.py`는 합격/불합격 harness가 아니다. 하드웨어·운영체제·Python 버전에
따라 달라지는 시간을 CI의 고정 임계값으로 사용하지 않는다. 대신 입력, 준비 실행 횟수, 표본 수,
측정 환경을 출력해 같은 조건에서 비교할 수 있게 한다.

측정 계층은 둘이다.

1. **새 프로세스 훅 전체** — 매 표본마다 Python 하위 프로세스를 새로 실행한다. 인터프리터
   시작, 모듈 import, stdin JSON 처리, 설정·사전 로드, 판정, stdout 직렬화를 포함한다.
2. **동일 프로세스 엔진 내부** — 엔진을 한 번 import한 뒤 설정 탐색, 사전 로드,
   `analyze()`를 각각 측정한다. 어느 단계가 입력 크기에 따라 늘어나는지 구분하기 위한 값이다.

각 시나리오는 준비 실행 5회 뒤 50회 측정한다. p95는 오름차순 표본의 nearest-rank 방식이다.
합성 대형 소스는 개인정보 별칭이나 암호화 호출이 없는 단순 메서드를 한 줄씩 생성한다.

## 기준선 — 2026-09-04

- 환경: macOS 26.4.1, arm64, Python 3.14.6
- 표본: 시나리오별 준비 실행 5회 + 측정 50회
- 대형 입력: 메모리 생성 합성 Java·Kotlin 각 2,000줄
- 시간 단위: ms

### 새 프로세스 훅 전체

| 시나리오 | 입력 | 중앙값 | p95 | 최소 | 최대 |
|---|---:|---:|---:|---:|---:|
| PreToolUse Write 적법 Java | 기존 합성 fixture 27줄 | 30.561 | 31.819 | 28.232 | 33.265 |
| PostToolUse Write 적법 Java | 기존 합성 fixture 27줄 | 30.374 | 32.192 | 29.430 | 32.339 |
| PreToolUse Write 차단 Java | 기존 합성 fixture 28줄 | 30.948 | 32.258 | 29.870 | 32.448 |
| PreToolUse Write 적법 Kotlin | 기존 합성 fixture 16줄 | 30.318 | 31.479 | 28.762 | 33.372 |
| PreToolUse Edit 대형 Java | 메모리 생성 합성 소스 2,000줄 | 142.101 | 149.019 | 139.874 | 156.982 |
| PostToolUse Edit 대형 Java | 메모리 생성 합성 소스 2,000줄 | 143.929 | 156.825 | 139.101 | 165.241 |
| PreToolUse Write 대형 Kotlin | 메모리 생성 합성 소스 2,000줄 | 126.367 | 130.616 | 123.744 | 132.216 |

### 동일 프로세스 엔진 내부

| 시나리오 | 입력 | 중앙값 | p95 | 최소 | 최대 |
|---|---:|---:|---:|---:|---:|
| 설정 탐색 | 프로젝트 루트에서 4단계 아래 | 0.024 | 0.025 | 0.021 | 0.026 |
| 항목 사전 로드 | 항목 9개 | 0.061 | 0.071 | 0.055 | 0.075 |
| analyze 적법 Java | 기존 합성 fixture 27줄 | 0.689 | 0.755 | 0.646 | 0.822 |
| analyze 차단 Java | 기존 합성 fixture 28줄 | 0.664 | 0.727 | 0.643 | 0.865 |
| analyze 대형 Java | 메모리 생성 합성 소스 2,000줄 | 112.195 | 113.420 | 110.997 | 123.892 |
| analyze 대형 Kotlin | 메모리 생성 합성 소스 2,000줄 | 95.331 | 97.101 | 94.248 | 97.495 |

## 해석과 다음 규칙의 제약

작은 fixture에서는 판정 자체가 1ms 미만이고 훅 전체가 약 30ms다. 설정 탐색과 사전 로드도
각각 0.1ms 미만이다. 따라서 작은 파일의 대부분은 새 Python 프로세스 시작과 모듈 import 등
고정비로 보인다. 이는 단계별 값을 비교한 **추론**이며 고정비만 별도로 직접 측정한 값은 아니다.

2,000줄 합성 입력에서는 Java `analyze()` 중앙값이 112.195ms, Kotlin이 95.331ms다. 훅 전체와의
차이도 작은 입력의 약 30ms 고정비와 비슷하다. 대형 파일에서는 `analyze()`가 주된 비용이다.

현재 수치만으로 Phase 2를 중단할 이유는 없다. 다만 적법한 `Edit`는 PreToolUse와 PostToolUse가
각각 파일 전체를 판정하므로 대형 Java 입력에서 두 훅이 각각 142.101ms, 143.929ms였다. 새 규칙은
파일 전체 정규식 순회를 무제한으로 추가하지 않고 기존 `prepare()` 결과와 얕은 전파 결과를
재사용해야 한다. `K-ENC-001` 구현 뒤 같은 명령을 다시 실행해 특히 다음 값을 비교한다.

- 작은 Java PreToolUse 전체와 `analyze()` p95
- 2,000줄 Java PreToolUse·PostToolUse 전체 p95
- 2,000줄 Java·Kotlin `analyze()` p95

실제 코드베이스에서 측정하더라도 결과는 공개하거나 이 문서에 옮기지 않는다. (D-07)

## `K-ENC-001` 활성화 후 — 2026-09-04

같은 환경·입력·준비 횟수·표본 수로 다시 측정했다. SQL 쓰기 신호가 없는 파일에서는
`K-ENC-001`의 값 전파 수집을 건너뛰는 빠른 경로를 적용한 결과다.

| 시나리오 | 추가 전 중앙값 | 추가 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 30.561ms | 32.267ms | +1.706ms (+5.6%) |
| 작은 적법 Java `analyze()` | 0.689ms | 0.711ms | +0.022ms (+3.2%) |
| 2,000줄 Java PreToolUse 전체 | 142.101ms | 146.429ms | +4.328ms (+3.0%) |
| 2,000줄 Java PostToolUse 전체 | 143.929ms | 147.020ms | +3.091ms (+2.1%) |
| 2,000줄 Java `analyze()` | 112.195ms | 114.636ms | +2.441ms (+2.2%) |
| 2,000줄 Kotlin PreToolUse 전체 | 126.367ms | 129.298ms | +2.931ms (+2.3%) |
| 2,000줄 Kotlin `analyze()` | 95.331ms | 97.849ms | +2.518ms (+2.6%) |

추가 후 p95는 작은 Java PreToolUse 전체 33.264ms, 2,000줄 Java PreToolUse 전체
147.933ms, PostToolUse 전체 150.063ms, Java `analyze()` 116.751ms, Kotlin `analyze()`
98.694ms였다. 측정 변동을 감안해 절대 임계값으로 판정하지 않지만, 새 규칙으로 인한 중앙값
증가는 모든 비교 항목에서 4.4ms 미만이었다. Phase 2의 다음 규칙도 같은 방식으로 전후를 비교한다.

## `K-ENC-003` 활성화 후 — 2026-09-05

같은 환경·입력·준비 횟수·표본 수로 다시 측정했다. 기존 시나리오는 기본 설정의
`subjectType=user`이므로 K-ENC-003의 빠른 종료 경로를 포함한다. 새 규칙 자체의 분기를 보기 위해
비이용자·내부망·영향평가 예외를 선언한 C026 합성 fixture 시나리오를 훅 전체와 엔진 내부에
추가했다. 실제 코드베이스 수치가 아니다.

| 시나리오 | K-ENC-001 후 중앙값 | K-ENC-003 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 32.267ms | 31.493ms | -0.774ms (-2.4%) |
| 작은 적법 Java `analyze()` | 0.711ms | 0.695ms | -0.016ms (-2.3%) |
| 2,000줄 Java PreToolUse 전체 | 146.429ms | 143.394ms | -3.035ms (-2.1%) |
| 2,000줄 Java PostToolUse 전체 | 147.020ms | 144.527ms | -2.493ms (-1.7%) |
| 2,000줄 Java `analyze()` | 114.636ms | 112.689ms | -1.947ms (-1.7%) |
| 2,000줄 Kotlin PreToolUse 전체 | 129.298ms | 128.082ms | -1.216ms (-0.9%) |
| 2,000줄 Kotlin `analyze()` | 97.849ms | 95.554ms | -2.295ms (-2.3%) |

추가한 비이용자 내부망 평가 예외 시나리오는 19줄 합성 fixture 기준으로 PreToolUse 전체
중앙값 30.544ms(p95 31.859ms), `analyze()` 중앙값 0.331ms(p95 0.367ms)였다. 기존 비교 항목은
모두 이전 측정보다 낮았지만 실행 시점의 변동이 있으므로 K-ENC-003이 성능을 개선했다고 해석하지
않는다. 측정 가능한 회귀가 나타나지 않았다는 결론만 낸다.

활성화 후 p95는 작은 Java PreToolUse 32.771ms, 2,000줄 Java PreToolUse 145.740ms,
PostToolUse 147.105ms, Java `analyze()` 113.772ms, Kotlin `analyze()` 96.518ms였다.

## `K-ENC-004` 활성화 후 — 2026-09-05

기존 시나리오와 같은 환경·입력·표본 조건으로 다시 측정했다. K-ENC-004의 실제 경로를 보기 위해
V025 평문 HTTP 전송 차단 fixture를 훅 전체와 엔진 내부 시나리오에 추가했다. 첫 구현에서 URL
판정용 표준 모듈 import가 새 프로세스 고정비를 늘리는 것이 보여, 필요한 HTTP 호스트 판정만
경량 코드로 바꾼 뒤 아래 최종 수치를 측정했다.

| 시나리오 | K-ENC-003 후 중앙값 | K-ENC-004 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 31.493ms | 32.792ms | +1.299ms (+4.1%) |
| 작은 적법 Java `analyze()` | 0.695ms | 0.719ms | +0.024ms (+3.5%) |
| 2,000줄 Java PreToolUse 전체 | 143.394ms | 147.724ms | +4.330ms (+3.0%) |
| 2,000줄 Java PostToolUse 전체 | 144.527ms | 147.683ms | +3.156ms (+2.2%) |
| 2,000줄 Java `analyze()` | 112.689ms | 114.877ms | +2.188ms (+1.9%) |
| 2,000줄 Kotlin PreToolUse 전체 | 128.082ms | 131.712ms | +3.630ms (+2.8%) |
| 2,000줄 Kotlin `analyze()` | 95.554ms | 96.978ms | +1.424ms (+1.5%) |

평문 HTTP 전송 차단 경로는 23줄 합성 fixture 기준 PreToolUse 전체 중앙값 32.895ms
(p95 34.506ms), `analyze()` 중앙값 0.602ms(p95 0.720ms)였다. 기존 비교 시나리오의 중앙값
증가는 모두 4.4ms 미만이다. 활성화 후 p95는 작은 Java PreToolUse 34.135ms, 2,000줄 Java
PreToolUse 149.785ms, PostToolUse 150.600ms, Java `analyze()` 116.830ms, Kotlin `analyze()`
98.656ms였다. 모두 합성 입력 수치이며 실제 코드베이스 성능을 나타내지 않는다.

## `K-ENC-004` DTO·TLS 경계 확장 후 — 2026-09-05

요청 DTO의 생성자·setter·builder 얕은 전파와 TLS 검증 무력화 신호를 추가한 뒤 같은 환경·입력·
표본 조건으로 다시 측정했다. 새 전파 경로 자체를 보기 위해 V031 요청 DTO 평문 HTTP 전송
fixture도 훅 전체와 엔진 내부 시나리오에 추가했다.

| 시나리오 | 확장 전 중앙값 | 확장 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 32.792ms | 32.324ms | -0.468ms (-1.4%) |
| 작은 적법 Java `analyze()` | 0.719ms | 0.707ms | -0.012ms (-1.7%) |
| 평문 HTTP 전송 차단 PreToolUse 전체 | 32.895ms | 33.607ms | +0.712ms (+2.2%) |
| 평문 HTTP 전송 차단 `analyze()` | 0.602ms | 0.942ms | +0.340ms (+56.5%) |
| 2,000줄 Java PreToolUse 전체 | 147.724ms | 147.370ms | -0.354ms (-0.2%) |
| 2,000줄 Java PostToolUse 전체 | 147.683ms | 147.826ms | +0.143ms (+0.1%) |
| 2,000줄 Java `analyze()` | 114.877ms | 113.935ms | -0.942ms (-0.8%) |
| 2,000줄 Kotlin PreToolUse 전체 | 131.712ms | 129.863ms | -1.849ms (-1.4%) |
| 2,000줄 Kotlin `analyze()` | 96.978ms | 96.098ms | -0.880ms (-0.9%) |

새 요청 DTO 차단 경로는 24줄 합성 fixture 기준 PreToolUse 전체 중앙값 32.914ms
(p95 35.185ms), `analyze()` 중앙값 1.031ms(p95 1.070ms)였다. 직접 HTTP 경로의 엔진 내부
비용은 0.340ms 늘었지만 새 프로세스 전체 중앙값 증가는 0.712ms였다. 대형 입력은 HTTP 클라이언트
문맥이 없으면 DTO 전파를 일찍 종료하므로 측정 가능한 회귀가 나타나지 않았다. 실행 시점 변동이
있으므로 감소한 값은 성능 개선으로 해석하지 않는다. 모두 합성 입력 수치이며 실제 코드베이스
성능을 나타내지 않는다.

## `K-ENC-004` HTTP 클라이언트 확장 후 — 2026-09-05

Spring `RestClient`, OkHttp와 Apache HttpClient 5의 명시적 송신 구조를 추가한 뒤 같은 환경·입력·
표본 조건으로 다시 측정했다. 세 클라이언트의 평문 HTTP 차단 fixture를 훅 전체와 엔진 내부
시나리오에 각각 추가했다.

| 시나리오 | 확장 전 중앙값 | 확장 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 32.324ms | 34.018ms | +1.694ms (+5.2%) |
| 작은 적법 Java `analyze()` | 0.707ms | 0.714ms | +0.007ms (+1.0%) |
| 평문 HTTP 전송 차단 PreToolUse 전체 | 33.607ms | 34.346ms | +0.739ms (+2.2%) |
| 평문 HTTP 전송 차단 `analyze()` | 0.942ms | 0.977ms | +0.035ms (+3.7%) |
| 요청 DTO 평문 HTTP 차단 PreToolUse 전체 | 32.914ms | 34.647ms | +1.733ms (+5.3%) |
| 요청 DTO 평문 HTTP 차단 `analyze()` | 1.031ms | 1.073ms | +0.042ms (+4.1%) |
| 2,000줄 Java PreToolUse 전체 | 147.370ms | 148.322ms | +0.952ms (+0.6%) |
| 2,000줄 Java PostToolUse 전체 | 147.826ms | 147.563ms | -0.263ms (-0.2%) |
| 2,000줄 Java `analyze()` | 113.935ms | 115.142ms | +1.207ms (+1.1%) |
| 2,000줄 Kotlin PreToolUse 전체 | 129.863ms | 131.750ms | +1.887ms (+1.5%) |
| 2,000줄 Kotlin `analyze()` | 96.098ms | 96.108ms | +0.010ms (+0.0%) |

새 API별 합성 경로는 다음과 같다.

| 시나리오 | 입력 | PreToolUse 중앙값 / p95 | `analyze()` 중앙값 / p95 |
|---|---:|---:|---:|
| Spring RestClient 평문 HTTP 차단 | 22줄 | 34.352ms / 35.859ms | 1.046ms / 1.141ms |
| OkHttp 평문 HTTP 차단 | 27줄 | 33.861ms / 35.726ms | 1.595ms / 1.716ms |
| Apache HttpClient 평문 HTTP 차단 | 24줄 | 33.892ms / 35.412ms | 1.832ms / 2.228ms |

기존 비교 시나리오의 중앙값 증가는 최대 1.887ms였다. Apache 경로가 세 API 중 엔진 내부 비용이
가장 컸지만 24줄 합성 입력에서 p95 2.228ms였다. 실제 코드베이스 성능과 체감은 미측정이다.

## 일반 개인정보 사전 확장 후 — 2026-09-05

사전을 9항목·별칭 89개에서 12항목·별칭 114개로 늘리고, V039 성명 평문 HTTP 차단 경로를
훅 전체와 엔진 내부 시나리오에 추가한 뒤 같은 환경·입력·표본 조건으로 다시 측정했다.

| 시나리오 | 확장 전 중앙값 | 확장 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 34.018ms | 35.486ms | +1.468ms (+4.3%) |
| 작은 적법 Java `analyze()` | 0.714ms | 0.705ms | -0.009ms (-1.3%) |
| 평문 HTTP 전송 차단 PreToolUse 전체 | 34.346ms | 36.443ms | +2.097ms (+6.1%) |
| 평문 HTTP 전송 차단 `analyze()` | 0.977ms | 0.970ms | -0.007ms (-0.7%) |
| 요청 DTO 평문 HTTP 차단 PreToolUse 전체 | 34.647ms | 36.557ms | +1.910ms (+5.5%) |
| 요청 DTO 평문 HTTP 차단 `analyze()` | 1.073ms | 1.063ms | -0.010ms (-0.9%) |
| 2,000줄 Java PreToolUse 전체 | 148.322ms | 151.822ms | +3.500ms (+2.4%) |
| 2,000줄 Java PostToolUse 전체 | 147.563ms | 151.444ms | +3.881ms (+2.6%) |
| 2,000줄 Java `analyze()` | 115.142ms | 115.487ms | +0.345ms (+0.3%) |
| 2,000줄 Kotlin PreToolUse 전체 | 131.750ms | 132.940ms | +1.190ms (+0.9%) |
| 2,000줄 Kotlin `analyze()` | 96.108ms | 98.713ms | +2.605ms (+2.7%) |

새 성명 차단 경로는 22줄 합성 fixture 기준 PreToolUse 전체 중앙값 36.663ms(p95 38.326ms),
`analyze()` 중앙값 0.824ms(p95 0.914ms)였다. 12항목 사전 로드는 중앙값 0.074ms,
p95 0.084ms였다. 기존 비교 시나리오의 중앙값 증가는 최대 3.881ms였고, 엔진 내부의 작은 HTTP
경로들은 감소하거나 0.010ms 이내 차이였다. 실행 시점 변동이 포함되므로 감소한 값은 성능 개선으로
해석하지 않는다. 모두 합성 입력 수치이며 실제 코드베이스 성능을 나타내지 않는다.

## `K-ENC-004` 선언형 HTTP 클라이언트 확장 후 — 2026-09-05

Spring HTTP Service·Retrofit·OpenFeign의 같은 파일 선언·프록시·실행 호출 연결과 JDK HttpClient
수신자 제한을 추가한 뒤 같은 환경에서 준비 실행 5회, 시나리오별 50회를 측정했다.

| 시나리오 | 중앙값 | p95 |
|---|---:|---:|
| 작은 적법 Java PreToolUse 전체 (27줄) | 34.224ms | 35.545ms |
| 작은 적법 Java `analyze()` | 0.705ms | 0.759ms |
| 평문 HTTP 전송 차단 PreToolUse 전체 (23줄) | 36.881ms | 38.899ms |
| 평문 HTTP 전송 차단 `analyze()` (23줄) | 1.021ms | 1.107ms |
| Spring HTTP Service 평문 HTTP 차단 PreToolUse 전체 (28줄) | 38.499ms | 49.289ms |
| Spring HTTP Service 평문 HTTP 차단 `analyze()` (28줄) | 1.931ms | 2.046ms |
| Retrofit 평문 HTTP 차단 PreToolUse 전체 (25줄) | 36.038ms | 38.945ms |
| Retrofit 평문 HTTP 차단 `analyze()` (25줄) | 1.257ms | 1.326ms |
| Feign 평문 HTTP 차단 PreToolUse 전체 (26줄) | 37.431ms | 39.320ms |
| Feign 평문 HTTP 차단 `analyze()` (26줄) | 1.571ms | 1.641ms |
| 대형 Java PreToolUse 전체 (2,000줄) | 147.361ms | 149.683ms |
| 대형 Java `analyze()` (2,000줄) | 114.944ms | 116.342ms |
| 대형 Kotlin PreToolUse 전체 (2,000줄) | 135.477ms | 146.267ms |
| 대형 Kotlin `analyze()` (2,000줄) | 96.276ms | 97.575ms |

선언형 세 경로의 `analyze()` 중앙값은 모두 2ms 미만이었다. 새 프로세스 전체 p95에는 Spring
HTTP Service 49.289ms처럼 실행 시점 변동이 포함됐지만, 같은 입력의 엔진 내부 p95는 2.046ms였다.
모두 합성 입력 수치이며 실제 코드베이스 성능이나 체감을 나타내지 않는다.

## `K-ENC-004` 서버 수신 TLS 범위 확장 후 — 2026-09-06

Spring 요청 매핑·요청 바인딩·개인정보 후보를 연결하고 `.pipa.json.inboundTransport`의 배포 경계
계약을 판정한 뒤 같은 환경에서 준비 실행 5회, 시나리오별 50회를 측정했다.

| 시나리오 | 중앙값 | p95 |
|---|---:|---:|
| 작은 적법 Java PreToolUse 전체 (27줄) | 36.639ms | 38.023ms |
| 작은 적법 Java `analyze()` | 0.714ms | 0.750ms |
| 평문 HTTP 전송 차단 PreToolUse 전체 (23줄) | 38.316ms | 40.224ms |
| 평문 HTTP 전송 차단 `analyze()` (23줄) | 1.045ms | 1.134ms |
| 서버 평문 HTTP 수신 차단 PreToolUse 전체 (17줄) | 36.637ms | 38.282ms |
| 서버 평문 HTTP 수신 차단 `analyze()` (17줄) | 0.364ms | 0.421ms |
| 대형 Java PreToolUse 전체 (2,000줄) | 154.351ms | 157.014ms |
| 대형 Java `analyze()` (2,000줄) | 116.462ms | 118.531ms |
| 대형 Kotlin PreToolUse 전체 (2,000줄) | 137.242ms | 139.769ms |
| 대형 Kotlin `analyze()` (2,000줄) | 97.891ms | 99.022ms |

새 서버 수신 경로는 짧은 조기 종료와 같은 파일 DTO 확인만 수행해 `analyze()` 중앙값이 0.364ms였다.
측정 시점 변동이 있으므로 이전 실행보다 낮아진 값은 개선으로 해석하지 않는다. 모두 합성 입력
수치이며 실제 코드베이스 성능이나 체감을 나타내지 않는다.

## `K-LEAK-001/002` 출력 최소화 활성화 후 — 2026-09-06

선언된 logger의 직접 인자와 Spring 컨트롤러 반환값·같은 파일 반환 DTO를
`.pipa.json.outputPolicies`와 비교하는 규칙을 추가한 뒤 같은 환경에서 준비 실행 5회,
시나리오별 50회를 측정했다. V055 API 출력 정책 초과 fixture를 훅 전체와 엔진 내부 시나리오에
추가했다.

| 시나리오 | 서버 수신 확장 후 중앙값 | 출력 최소화 활성화 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 36.639ms | 40.288ms | +3.649ms (+10.0%) |
| 작은 적법 Java `analyze()` | 0.714ms | 0.751ms | +0.037ms (+5.2%) |
| 평문 HTTP 전송 차단 PreToolUse 전체 | 38.316ms | 40.979ms | +2.663ms (+7.0%) |
| 평문 HTTP 전송 차단 `analyze()` | 1.045ms | 1.070ms | +0.025ms (+2.4%) |
| 2,000줄 Java PreToolUse 전체 | 154.351ms | 162.406ms | +8.055ms (+5.2%) |
| 2,000줄 Java PostToolUse 전체 | 154.173ms | 161.946ms | +7.773ms (+5.0%) |
| 2,000줄 Java `analyze()` | 116.462ms | 120.057ms | +3.595ms (+3.1%) |
| 2,000줄 Kotlin PreToolUse 전체 | 137.242ms | 143.671ms | +6.429ms (+4.7%) |
| 2,000줄 Kotlin `analyze()` | 97.891ms | 101.759ms | +3.868ms (+4.0%) |

새 API 출력 정책 초과 경로는 21줄 합성 fixture 기준 PreToolUse 전체 중앙값 40.432ms
(p95 41.617ms), `analyze()` 중앙값 0.610ms(p95 0.676ms)였다. 활성화 후 대형 Java
PreToolUse p95는 165.320ms, PostToolUse p95는 165.070ms, Java `analyze()` p95는
122.760ms, Kotlin `analyze()` p95는 103.985ms였다.

기존 비교 항목은 이번 실행에서 모두 증가했고 최대 증가는 새 프로세스 전체 8.055ms,
엔진 내부 3.868ms였다. 출력 규칙은 Spring 컨트롤러나 logger 문맥이 없으면 즉시 종료하지만
문맥 확인 자체는 파일 전체를 본다. 측정 시점 변동과 규칙 비용을 분리해 측정하지 않았으므로
증가분 전부를 규칙 비용이라고 단정하지 않는다. 다음 규칙에서도 같은 입력으로 추세를 확인한다.
모두 합성 입력 수치이며 실제 코드베이스 성능이나 체감을 나타내지 않는다.

## `K-LOG-001` 접속기록 구성요소 초기 범위 활성화 후 — 2026-09-06

`.pipa.json.accessLogPolicies`가 지정한 logger 호출에서 제2조 제3호의 다섯 구성요소를 확인하는
규칙을 추가한 뒤 같은 환경에서 준비 실행 5회, 시나리오별 50회를 측정했다. V063 처리 정보주체
누락 fixture를 훅 전체와 엔진 내부 시나리오에 추가했다.

| 시나리오 | 출력 최소화 활성화 후 중앙값 | 접속기록 규칙 활성화 후 중앙값 | 변화 |
|---|---:|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 40.288ms | 36.032ms | -4.256ms (-10.6%) |
| 작은 적법 Java `analyze()` | 0.751ms | 0.742ms | -0.009ms (-1.2%) |
| 평문 HTTP 전송 차단 PreToolUse 전체 | 40.979ms | 36.710ms | -4.269ms (-10.4%) |
| 평문 HTTP 전송 차단 `analyze()` | 1.070ms | 1.038ms | -0.032ms (-3.0%) |
| 2,000줄 Java PreToolUse 전체 | 162.406ms | 153.006ms | -9.400ms (-5.8%) |
| 2,000줄 Java PostToolUse 전체 | 161.946ms | 153.601ms | -8.345ms (-5.2%) |
| 2,000줄 Java `analyze()` | 120.057ms | 116.736ms | -3.321ms (-2.8%) |
| 2,000줄 Kotlin PreToolUse 전체 | 143.671ms | 135.406ms | -8.265ms (-5.8%) |
| 2,000줄 Kotlin `analyze()` | 101.759ms | 97.380ms | -4.379ms (-4.3%) |

새 접속기록 구성요소 누락 경로는 16줄 합성 fixture 기준 PreToolUse 전체 중앙값 36.228ms
(p95 37.591ms), `analyze()` 중앙값 0.416ms(p95 0.434ms)였다. 활성화 후 대형 Java
PreToolUse p95는 154.798ms, PostToolUse p95는 156.505ms, Java `analyze()` p95는
118.189ms, Kotlin `analyze()` p95는 98.821ms였다.

기존 비교 항목은 모두 직전 실행보다 낮았지만 실행 시점 변동이므로 성능 개선으로 해석하지
않는다. `accessLogPolicies`가 없으면 logger 수신자와 메서드 범위를 수집하기 전에 종료하므로
이번 합성 측정에서는 회귀가 나타나지 않았다는 결론만 낸다. 모두 합성 입력 수치이며 실제
코드베이스 성능이나 체감을 나타내지 않는다.

## D-41 암호 전파 경계 수정 — 2026-09-09 소표본 점검

macOS 26.4.1 arm64·Python 3.14.6에서 `python3 tests/benchmark_hooks.py --samples 5 --warmups 1`을
실행했다. 기존 50회 기준선과 표본 수·시점이 다르므로 직접적인 성능 개선·회귀율로 해석하지 않는다.
모두 합성 fixture 또는 메모리 생성 합성 소스 기준이다.

| 시나리오 | 중앙값 | p95 |
|---|---:|---:|
| 작은 적법 Java PreToolUse 전체 | 41.961ms | 42.903ms |
| 작은 적법 Java analyze | 0.909ms | 1.003ms |
| 2,000줄 Java PreToolUse 전체 | 218.724ms | 219.223ms |
| 2,000줄 Java analyze | 177.028ms | 178.772ms |
| 2,000줄 Kotlin PreToolUse 전체 | 203.140ms | 204.700ms |

새 메서드 범위·인자 의존 분석의 비용이 추가되었다. 앞선 기준선보다 대형 입력 시간이 높으므로
배포 전 동일 환경·표본 수로 변경 전후를 비교할 후속 항목으로 남긴다. 이번 결과만으로 실제
프로젝트 성능이나 최악 실행 시간을 보장하지 않는다.
